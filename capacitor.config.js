const config = {
  appId: 'com.example.app',
  appName: 'homza',
  webDir: 'out',
  server: {
    url: '', // Set to your dev server URL if needed, else leave blank for production
    cleartext: true // Allow HTTP connections (useful for local dev)
  }
};

module.exports = config;
